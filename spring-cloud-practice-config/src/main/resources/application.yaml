server:
  port: 8088
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka
spring:
  boot:
    admin:
      client:
        url: http://localhost:8899
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: D:/Projects/Java/spring-cloud-practice/spring-cloud-practice-config-repository/{application}
